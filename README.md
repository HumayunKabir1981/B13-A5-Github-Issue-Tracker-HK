1️⃣ What is the difference between var, let, and const?
    var: var is the old way to declare variables. Now a days var is not use.
    let: let was introduced in ES6 and is the modern way to declare variables.
    const: const is used for constant variables.


2️⃣ What is the spread operator (...)?
    The spread operator (...) in JavaScript is used to expand (spread out) elements of an array, object, or iterable into individual elements.



3️⃣ What is the difference between map(), filter(), and forEach()?
    map(): map() is used to transform each element of an array and returns a new array.its Returns a new array
    filter(): filter() is used to select elements based on a condition. its Returns a new array
    forEach(): forEach()is used to loop through an array and perform an action for each element.
    

4️⃣ What is an arrow function?
    An arrow function is a shorter syntax for writing functions in JavaScript, introduced in ES6.


5️⃣ What are template literals?
    Template literals are a feature in JavaScript ES6  that allow you to create strings more easily using backticks instead of quotes.
    its inside Insert variables into strings. Write multi-line strings.